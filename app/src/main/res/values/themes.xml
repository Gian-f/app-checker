<resources>
    <!-- Base application theme. -->
    <style name="Theme.AppChecker" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/color_primary_high</item>
        <item name="colorPrimaryVariant">@color/color_primary_medium</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/color_secondary_medium</item>
        <item name="colorSecondaryVariant">@color/color_secondary_light</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <!-- Customize your theme here. -->
    </style>
    <!-- Theme for OnBoardingActivity (applying some transition) -->
    <style name="Theme.AppChecker.OnBoarding">
        <item name="android:windowSharedElementsUseOverlay">false</item>
        <item name="android:windowContentTransitions">true</item>

        <item name="android:windowExitTransition">@transition/activity_main_exit</item>
        <item name="android:windowReenterTransition">@transition/activity_main_reenter</item>
    </style>
    <!--Styling TextInputLayout colors-->
    <style name="EditText.Login" parent="Widget.AppCompat.EditText">
        <item name="colorControlNormal">@color/gray</item>
        <item name="colorControlActivated">@color/gray</item>
    </style>

    <style name="Widget.MaterialComponents.Button.TextButton.OnBoardTextButton" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/color_primary_high</item>
    </style>

    <style name="Theme.AppChecker.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    
    <style name="CustomChipStyle" parent="Widget.Material3.Chip.Assist.Elevated"> 
        <item name="chipBackgroundColor">#F0F0F0</item>
        <item name="chipCornerRadius">10dp</item>

    </style>

    <style name="Theme.AppChecker.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.AppChecker.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>